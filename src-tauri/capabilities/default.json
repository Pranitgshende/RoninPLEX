{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for CinePulse desktop window",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
