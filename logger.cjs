const http = require('http'); http.createServer((req, res) => { let body = ''; req.on('data', chunk => body += chunk.toString()); req.on('end', () => { console.log(body); res.setHeader('Access-Control-Allow-Origin', '*'); res.end('ok'); }); }).listen(9999, () => console.log('Logger listening on 9999'));